# help: https://github.com/anishathalye/dotbot#configuration
- defaults:
     link:
      relink: true

- clean: ['~'] # clean dead symlinks

- link: # Symlinks
    ~/.zshrc:
      force: true
      path: .zshrc